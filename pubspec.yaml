name: track_fitness
description: "A new Flutter project."

publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.3.3 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  clay_containers: ^0.3.4
  fl_chart: ^0.68.0
  flutter_screenutil: ^5.9.3
  carousel_slider: ^5.0.0
  get: ^4.6.6
  cached_network_image: ^3.4.1
  flutter_animate: ^4.5.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

  assets:
    - lib/Assets/images/

  fonts:
    - family: g_font
      fonts:
        - asset: lib/Assets/fonts/g_font.ttf
